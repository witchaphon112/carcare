# carwash.API — Agent Notes

ASP.NET Core minimal API (`net9.0`). Currently still close to template scaffolding.

## Run

- `dotnet run`

## Current behavior

- Exposes `GET /weatherforecast` (template).

## Conventions

- Prefer minimal, incremental changes.
- If adding auth/login later, document the chosen flow and endpoints in this folder.

